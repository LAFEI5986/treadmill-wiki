# 跑步机行业知识库 · 发布指南

Quartz v5 站点，内容来自 Obsidian 笔记库
`C:\hermes-web-ui\hermes_data\Desktop\米粒\跑步机行业研究`。

## 日常：改完笔记怎么更新网站

站点已经上线在 **https://lafei5986.github.io/treadmill-wiki/** 。
在 Obsidian 里正常写笔记，然后在本目录（`C:\Users\lzs01\Desktop\稽查\quartz`）执行：

```powershell
.\publish.ps1
```

一条命令走完全流程：同步笔记库 → 提交 → 推送 → GitHub Actions 自动构建发布，
约 1-2 分钟后线上生效。提交信息默认带时间戳，想自己写就跟在后面：

```powershell
.\publish.ps1 "补充 Peloton 财务数据"
```

发之前想先在本地看看效果：

```powershell
.\publish.ps1 -Preview
```

这会同步并起本地预览（http://localhost:8080），**不会**推送。看完 Ctrl+C 结束，
确认没问题再跑一次不带 `-Preview` 的版本正式发布。

笔记没有任何改动时脚本会提示"无需发布"直接退出，不会产生空提交。

### 底层的两个脚本

- `sync-vault.ps1` —— 只负责笔记库 → `content\` 的镜像同步（`robocopy /MIR`），
  可单独用：`-Serve` 同步并预览，`-NoBuild` 只同步不构建。
- `publish.ps1` —— 调用上面那个，再加上 git 提交推送。

`content\` 是笔记库的镜像副本，**不要直接在里面改笔记**——下次同步会被 `/MIR` 覆盖删除。
笔记库路径变了就改 `sync-vault.ps1` 顶部的 `$Vault` 默认值。

### 发布失败时看哪里

- **连接被重置** —— 代理没开，或代理端口变了。git 不读 Windows 系统代理，只认
  `git config --global http.proxy`（当前设为 `http://127.0.0.1:7897`）。
- **要求输入用户名密码** —— GitHub 授权过期，重新推送时会弹浏览器；若不弹，去
  Windows 凭据管理器删掉 `git:https://github.com` 条目后重试。
- **推送成功但网站没变** —— 去 https://github.com/LAFEI5986/treadmill-wiki/actions
  看构建是否失败，点进去看红叉那一步的日志。

## 首次发布到 GitHub Pages

本站已按以下参数配置好，`baseUrl` 和 git remote 都不用再改：

| 项 | 值 |
|---|---|
| GitHub 账号 | `LAFEI5986` |
| 仓库名 | `treadmill-wiki` |
| `baseUrl` | `lafei5986.github.io/treadmill-wiki` |
| remote origin | `https://github.com/LAFEI5986/treadmill-wiki.git` |
| 最终网址 | `https://lafei5986.github.io/treadmill-wiki/` |

两个容易踩的点：GitHub Pages 域名**恒为小写**，所以 baseUrl 里写 `lafei5986` 而非
`LAFEI5986`；项目仓库的 baseUrl 必须带 `/treadmill-wiki` 这一段，漏了会导致自托管字体
等资源的绝对地址指错。

要改仓库名的话，同时改这两处：`quartz.config.yaml` 的 `baseUrl`，以及
`git remote set-url origin https://github.com/LAFEI5986/新名字.git`。

### 1. 在 GitHub 上建空仓库

github.com 右上角 `+` → New repository → Repository name 填 `treadmill-wiki` →
选 Public → **Add a README / .gitignore / license 三个勾一个都别勾**（勾了远端会先有一次
提交，推送会冲突）→ Create repository。

GitHub Pages 从**私有仓库**发布需要 Pro/Team 套餐。免费账号选 Public 就意味着笔记原文
公开可见，介意的话改用 Cloudflare Pages。

### 2. 推送

```powershell
git push -u origin main
```

首次执行会弹浏览器让你授权 GitHub（已配好 `credential.helper = manager`，不需要手动建 token）。

### 3. 打开 Pages

仓库 → Settings → 左侧 Pages → "Build and deployment" 的 Source 选 **GitHub Actions**
（不是 Deploy from a branch）。

然后在 Actions 标签页看 "Deploy Quartz site to GitHub Pages" 跑完，约 1-2 分钟。

### 4. 以后的发布流程

见开头的「日常」一节，跑 `.\publish.ps1` 即可。

## 这个站点做过哪些针对性配置

- **链接解析用 `relative`**：笔记里有 45 条 `[[../folder/note]]` 形式的相对链接，
  Quartz 默认的 `shortest` 模式会把它们解析到错误的层级（少上跳一层）。
  代价是：**以后在子目录的笔记里，不要写 `[[wiki/xxx/yyy]]` 这种从库根开始的路径**，
  那样在 relative 模式下会失效；写相对路径或同目录的裸文件名。
  根目录的 `Home.md`、`index.md` 不受此限。

- **所有带路径的 wikilink 都补了 `|别名`**：Quartz 有个缺陷，判断链接是否带别名时
  拿已百分号编码的 href 和未编码的显示文字比较，中文必然不相等，于是跳过了
  "只显示文件名"的处理，链接会显示成 `../concepts/家用跑步机` 这样的完整路径。
  补上显式别名即可绕开（Obsidian 里阅读也更清爽）。
  **新写带路径的中文链接时，记得也带上 `|别名`。**

- **中文字体回退**：见 `quartz\styles\custom.scss`。Quartz 默认的三款西文字体都没有
  汉字字形，且 `quartz-fonts` 插件输出的 `h1..h6` 规则和 `--h*-font` 变量是写死的
  裸字体名，必须在 custom.scss 里覆盖，否则中文标题会掉到宋体。

- **字体构建时自托管**（`cdnCaching: false`）：不依赖 Google Fonts CDN，国内可访问。

- **界面语言 zh-CN**，已关闭 Quartz 默认开启的 Plausible 统计。

- **`cname` 插件已关闭**：它会按 baseUrl 生成 CNAME 文件，而 GitHub Pages 把 CNAME 当作
  「自定义域名」。用 `xxx.github.io/仓库名` 这种子路径托管时带上它会导致部署冲突。
  将来真绑自定义域名时再把 `enabled` 改回 `true`。

- **`wiki/test.md` 已排除**发布，见 `ignorePatterns`。

## 已知的内容缺口

以下笔记被引用但尚未创建，站点上显示为灰色虚线加 `?`：

- `wiki/players/NordicTrack`
- `wiki/players/Life-Fitness`
- `wiki/players/亿健`
- `wiki/concepts/家庭健身趋势`
- `wiki/maps/行业研究框架`

补上笔记后重新同步即可，无需改配置。

## 升级 Quartz

```powershell
git pull upstream v5
npm install
```

`quartz\styles\custom.scss` 是本地改过的文件，升级时可能冲突，注意保留其中的中文字体回退。
